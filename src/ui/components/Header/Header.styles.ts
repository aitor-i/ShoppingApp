import styled from "styled-components";

const Wrapper = styled.div`
  font-family: Arial;
  text-align: center;
  h1 {
    font-size: 36px;
  }
  h2 {
    font-style: italic;
  }
`;

export default Wrapper;
